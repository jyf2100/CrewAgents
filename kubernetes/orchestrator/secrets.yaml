apiVersion: v1
kind: Secret
metadata:
  name: hermes-orchestrator-secret
  namespace: hermes-agent
type: Opaque
# Do NOT put real values here. Populate via:
#   kubectl create secret generic hermes-orchestrator-secret \
#     --from-literal=ORCHESTRATOR_API_KEY="$(openssl rand -hex 32)" \
#     --namespace hermes-agent --dry-run=client -o yaml | kubectl apply -f -
