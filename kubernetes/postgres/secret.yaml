apiVersion: v1
kind: Secret
metadata:
  name: hermes-db-secret
  namespace: hermes-agent
type: Opaque
stringData:
  username: hermes
  # 部署前必须替换。生成方法：openssl rand -base64 24 | tr -d '\n'
  password: CHANGE_ME
